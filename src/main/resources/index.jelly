<!--
  This view is used to render the installed plugins page.
-->
<div>
  This plugin publishes JMH benchmark result to each build and provides a visualization for trending benchmark result across build. It also compares performance degradation/gain of a benchmark between successive builds and mark a build unstable if the degradation is below the specified threshold.
</div>
